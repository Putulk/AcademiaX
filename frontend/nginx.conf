server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router SPA — any path not matching a real static file falls
    # back to index.html so client-side routing handles it.
    location / {
        try_files $uri /index.html;
    }
}
